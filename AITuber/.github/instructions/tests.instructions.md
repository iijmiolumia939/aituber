---
applyTo: "**/tests/**,**/*test*.py"
---
# Tests instructions (SRS mapped)

- 会話・コメントは日本語。識別子は英語。

Every new/changed behavior must map to a TC-* in `.github/srs/tests.yml`,
or you must add a new TC-* entry alongside the test.
